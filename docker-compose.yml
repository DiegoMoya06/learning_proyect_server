version: '3.8'

services:
#  TODO: check
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/your_database
#      SPRING_DATASOURCE_USERNAME: root
#      SPRING_DATASOURCE_PASSWORD: rootpassword
#    depends_on:
#      - db

  db:
    image: mysql:8.0
    container_name: mysql-db
    restart: always
    environment:
#      TODO: will use environment variables in future
      MYSQL_PASSWORD: x#2M8e)#m0@V1J+>J(!3
      MYSQL_DATABASE: learning_cards
      MYSQL_USER: DB_USR_1
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: